{% extends 'base.html.twig' %}

{% block title %}New Recipe{% endblock %}

{% block body %}
    <h1>Create new Recipe</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Create</button>
    {{ form_end(form) }}
    <a href="{{ path('user.recipe.index') }}">back to list</a>
{% endblock %}
